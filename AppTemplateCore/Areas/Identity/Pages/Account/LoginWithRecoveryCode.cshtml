@page
@model LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "Recovery code verification";
}

<h1>@ViewData["Title"]</h1>
<hr />
<p>

    When you don't have access to your authenticator device then
    you come to this SCREEN so that
    you can log in with a RECOVERY CODE
    You have requested to log in with a RECOVERY CODE.
    This login will not be remembered until you provide
    an authenticator app code at log in or disable 2FA and log in again.
    MEANS SYSTEM WILL FORGET LOGIN AFTER 30 MINTS OR SO IF SESSION IS EXPIRED.

    @*
        // RECOVERY CODES ARE GENERATED BY USERMANAGER.
        // THERE IS NO INVOLVEMENT OF AUTHENTICATOR APP IN CASE OF LOGIN WITH RECOVER CODE
    *@

</p>
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Input.RecoveryCode"></label>
                <input asp-for="Input.RecoveryCode" class="form-control" autocomplete="off" />
                <span asp-validation-for="Input.RecoveryCode" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-primary">Log in</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
